{
  "name": "azf-acos-interact",
  "version": "1.29.1",
  "description": "Handles forms from Acos",
  "scripts": {
    "start": "func start",
    "test": "biome check && node --test tests/*.test.js",
    "lint": "biome check",
    "lint:fix": "biome check --write",
    "cert:generate": "openssl req -x509 -newkey rsa:4096 -keyout cert/privatekey.key -out cert/certificate.pem -days 365 -nodes -subj \"/CN=VFK-ACOS-DEV\" && openssl pkcs12 -export -legacy -out cert/certificate.pfx -inkey cert/privatekey.key -in cert/certificate.pem -passout pass:"
  },
  "dependencies": {
    "@azure/msal-node": "^5.2.2",
    "@vestfoldfylke/loglady": "^1.5.3",
    "@vtfk/azure-blob-client": "^0.1.7",
    "axios": "^1.13.6",
    "dotenv": "^16.6.1",
    "file-system-cache": "^2.4.7",
    "node-cache": "^5.1.2",
    "node-forge": "^1.4.0",
    "xml2js": "^0.5.0"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.9"
  }
}
